import {Modal} from "antd";

export function CreateRoomModal({visible}) {
    return <Modal width={300} height={400} title={'방 만들기'} visible={visible}>
        asdf
    </Modal>

}

export default CreateRoomModal